<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidThemes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AndroidThemes" parent="Base.Theme.AndroidThemes" >
        <item name="android:layout_margin">10dp</item>
        <item name="android:textSize">18sp</item>
        <item name="colorPrimary">@color/red</item>
        <item name="backgroundColor">@color/grey</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <color name="grey">#8D8D8D</color>
    <color name="red">#FF0000</color>
</resources>